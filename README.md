# SchoolduleDemo

Implementation of [**ng-schooldule**](https://github.com/rbwrightjr91/ng-schooldule).

Clone demo into desired local directory. Then, `cd` into the folder and run `npm install` and `ng serve`.
